CREATE TABLE Profile(
    name TEXT NOT NULL PRIMARY KEY,
    isCommon INTEGER AS Boolean NOT NULL DEFAULT 0
);

selectAll:
SELECT *
FROM Profile;

insert:
INSERT INTO Profile(name, isCommon)
VALUES ?;

delete:
DELETE FROM Profile
WHERE name = ?;
